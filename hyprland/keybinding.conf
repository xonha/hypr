#
#-- Keybindings ------------------------------------------------
$menu        = ~/.config/hypr/.scripts/menu
$volume      = ~/.config/hypr/.scripts/volume
$powermenu   = ~/.config/hypr/.scripts/powermenu
$backlight   = ~/.config/hypr/.scripts/brightness
$screenshot  = ~/.config/hypr/.scripts/screenshot
$lockscreen  = ~/.config/hypr/.scripts/lockscreen
$colorpicker = ~/.config/hypr/.scripts/colorpicker

$term		 = kitty --config $HOME/.config/hypr/kitty/kitty.conf
$explorer    = nemo
$text_editor = geany
$browser     = google-chrome-stable

# -- System Apps --
bindr = $SUPER, $SUPER_L, exec, $menu
bind  = $SUPER, Return,   exec, $term 
bind  = $SUPER, E,		  exec, $explorer
bind  = $SUPER, T,		  exec, $text_editor
bind  = $SUPER, B,		  exec, $browser

# -- Apps --
bind = $SUPER, C, exec, code
bind = $SUPER, K, exec, keeweb
bind = $SUPER, D, exec, discord

# -- Misc --
bind = $SUPER, Escape, exec, $powermenu
bind = $SUPER, P,      exec, $colorpicker
bind = $SUPER, A,      exec, $lockscreen
bind = $SUPER, N,      exec, nm-connection-editor
bind = $SUPER, M,      exec, $volume --toggle-mic

# -- Function keys --
bind = ,XF86MonBrightnessUp,   exec, $backlight --inc
bind = ,XF86MonBrightnessDown, exec, $backlight --dec
bind = ,XF86AudioRaiseVolume,  exec, $volume --inc
bind = ,XF86AudioLowerVolume,  exec, $volume --dec
bind = ,XF86AudioMute,         exec, $volume --toggle
bind = ,XF86AudioMicMute,      exec, $volume --toggle-mic
bind = ,XF86AudioNext,         exec, mpc next
bind = ,XF86AudioPrev,         exec, mpc prev
bind = ,XF86AudioPlay,         exec, mpc toggle
bind = ,XF86AudioStop,         exec, mpc stop

# -- Screenshots --
bind = ,       Print, exec, $screenshot --now
bind = ALT,    Print, exec, $screenshot --in5
bind = SHIFT,  Print, exec, $screenshot --in10
bind = CTRL,   Print, exec, $screenshot --win
bind = $SUPER, Print, exec, $screenshot --area
bind = $SUPER, S, 	  exec, $screenshot --area

# -- Hyprland --
bind = $SUPER,	   Q,	  killactive,
bind = $SUPER,	   F,	  fakefullscreen,	   0
bind = $SUPER, 	   Space, movetoworkspace, empty
bind = $SUPER_ALT, Space, togglefloating,
bind = $SUPER_ALT, Space, centerwindow,

# Change Focus
bind = $SUPER_ALT, left,  movefocus, l
bind = $SUPER_ALT, right, movefocus, r
bind = $SUPER_ALT, up,    movefocus, u
bind = $SUPER_ALT, down,  movefocus, d

bind = $SUPER_ALT, H, movefocus, l
bind = $SUPER_ALT, L, movefocus, r
bind = $SUPER_ALT, K, movefocus, u
bind = $SUPER_ALT, J, movefocus, d

# Move Active
bind = $SUPER_SHIFT, left,  movewindow, l
bind = $SUPER_SHIFT, right, movewindow, r
bind = $SUPER_SHIFT, up,    movewindow, u
bind = $SUPER_SHIFT, down,  movewindow, d

# Resize Active
binde = $SUPER_CTRL, left,  resizeactive, -20 0
binde = $SUPER_CTRL, right, resizeactive, 20 0
binde = $SUPER_CTRL, up,    resizeactive, 0 -20
binde = $SUPER_CTRL, down,  resizeactive, 0 20

# Move Active (Floating Only)
binde = $SUPER_SHIFT, left,  moveactive, -20 0
binde = $SUPER_SHIFT, right, moveactive, 20 0
binde = $SUPER_SHIFT, up,    moveactive, 0 -20
binde = $SUPER_SHIFT, down,  moveactive, 0 20

# Switch Between Workspaces
bind = $SUPER, left,  focusmonitor, $MONITOR2
bind = $SUPER, left,  workspace, 	m-1
bind = $SUPER, right, focusmonitor, $MONITOR2
bind = $SUPER, right, workspace, 	m+1

bind = $SUPER, up,	 focusmonitor, $MONITOR1
bind = $SUPER, up,	 workspace, m-1
bind = $SUPER, down, focusmonitor, $MONITOR1
bind = $SUPER, down, workspace, m+1

bind = $SUPER, H, focusmonitor, $MONITOR2
bind = $SUPER, H, workspace, 	m-1
bind = $SUPER, J, focusmonitor, $MONITOR2
bind = $SUPER, J, workspace, 	m+1

bind = $SUPER, V, focusmonitor, $MONITOR1
bind = $SUPER, V, workspace,	m-1
bind = $SUPER, L, focusmonitor, $MONITOR1
bind = $SUPER, L, workspace, 	m+1


# Workspaces
bind = $SUPER, 1, workspace, 1
bind = $SUPER, 2, workspace, 2
bind = $SUPER, 3, workspace, 3
bind = $SUPER, 4, workspace, 4
bind = $SUPER, 5, workspace, 5
bind = $SUPER, 6, workspace, 6
bind = $SUPER, 7, workspace, 7
bind = $SUPER, 8, workspace, 8
bind = $SUPER, 9, workspace, 9

# Send to Workspaces
bind = $SUPER_CTRL, 1, movetoworkspace, 1
bind = $SUPER_CTRL, 2, movetoworkspace, 2
bind = $SUPER_CTRL, 3, movetoworkspace, 3
bind = $SUPER_CTRL, 4, movetoworkspace, 4
bind = $SUPER_CTRL, 5, movetoworkspace, 5
bind = $SUPER_CTRL, 6, movetoworkspace, 6
bind = $SUPER_CTRL, 7, movetoworkspace, 7
bind = $SUPER_CTRL, 8, movetoworkspace, 8
bind = $SUPER_CTRL, 9, movetoworkspace, 9

# Misc
bindl = ,switch:Lid Switch, exec, $lockscreen
bind  = $SUPER_SHIFT,		P, pin,
bind  = $SUPER_SHIFT,		P, exec, $notifycmd 'Toggled Pin'
bind  = $SUPER_SHIFT,		S, swapnext
bind  = $SUPER_SHIFT,		O, toggleopaque

#-- Mouse Buttons ----------------------------------------------
bindm = $SUPER, mouse:272,  movewindow
bindm = $SUPER, mouse:273,  resizewindow
bind  = $SUPER, mouse_down, workspace, e+2
bind  = $SUPER, mouse_up,   workspace, e-2